# Web 与测试（与 CI、bootstrap 对齐；版本下限可随安全公告上调）
fastapi>=0.109
uvicorn[standard]>=0.27
pydantic>=2
httpx
playwright>=1.49

# PDF：`pdf_meta` / `pdf_read` 等优先 PyMuPDF；无则回退 poppler/pypdf（CI 与全技能一致）
pymupdf>=1.24.0

# 本地 STT：`audio_transcribe*` 与 `video_summarize` 音轨转写依赖 Apple MLX 官方包。
# 仅 Apple Silicon macOS 有 PyPI 轮子；Linux/Intel Mac 不会安装此行（CI 不受影响）。
mlx-whisper>=0.4.0; sys_platform == "darwin" and platform_machine == "arm64"
